# <PROJECT_NAME> Build and Release Notes

This document describes the current build, development, and packaging-related behavior in the repository.

## 1) Tooling Baseline

- <TOOLING_1>
- <TOOLING_2>
- <TOOLING_3>

## 2) Main Project Commands

Document the canonical commands:

- development: `<DEV_COMMAND>`
- build: `<BUILD_COMMAND>`
- lint: `<LINT_COMMAND>`
- format: `<FORMAT_COMMAND>`
- combined check: `<CHECK_COMMAND>`

If any of these modify files, say so explicitly.

## 3) Build Structure

- entry: `<BUILD_ENTRY>`
- output: `<BUILD_OUTPUT>`
- important packaging behavior: <PACKAGING_BEHAVIOR>

## 4) Runtime Asset Packaging

Document local runtime assets only if the project has them.

- <RUNTIME_ASSET_1>
- <RUNTIME_ASSET_2>

## 5) Helper Scripts

List only the helper scripts that matter in practice:

- `<SCRIPT_1>`: <BEHAVIOR>
- `<SCRIPT_2>`: <BEHAVIOR>

## 6) Recommended Commands

State the commands contributors should normally use.

## 7) Release Reality

If the project does not yet have a formal release runbook, say so directly.
